{
  "name": "universal-web-ai-orchestrated-agent",
  "displayName": "Universal Web AI Agent for VS Code",
  "description": "Embedded WebSocket Bridge Server & Autonomous Coding Executor for Web LLMs (ChatGPT, Claude, Gemini, DeepSeek, OpenWebUI, Custom Enterprise LLMs)",
  "version": "2.1.0",
  "publisher": "web-ai-agent",
  "engines": {
    "vscode": "^1.80.0"
  },
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "webAgent.startServer",
        "title": "AI Agent: Connect Bridge Server"
      },
      {
        "command": "webAgent.stopServer",
        "title": "AI Agent: Disconnect Bridge Server"
      },
      {
        "command": "webAgent.restartServer",
        "title": "AI Agent: Restart Bridge Server"
      },
      {
        "command": "webAgent.toggleServer",
        "title": "AI Agent: Manage Bridge Server"
      },
      {
        "command": "webAgent.showLogs",
        "title": "AI Agent: Show Output Channel"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Universal Web AI Agent",
      "properties": {
        "webAgent.serverPort": {
          "type": "number",
          "default": 9999,
          "description": "Port on which the embedded WebSocket Bridge Server listens"
        },
        "webAgent.autoStartServer": {
          "type": "boolean",
          "default": false,
          "description": "Automatically launch the embedded Bridge Server on VS Code startup"
        }
      }
    }
  },
  "scripts": {
    "postinstall": "npm run compile",
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "build": "npm run compile",
    "package": "npx @vscode/vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "@types/ws": "^8.5.10",
    "@vscode/vsce": "^2.24.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}